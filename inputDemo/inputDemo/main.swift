import Foundation

print("\nenter three integers")
let nums = [Int(Double(readLine() ?? 0)),
            Int(Double(readLine() ?? "0") ?? 0),
            Int(Double(readLine() ?? "0") ?? 0)]
print(nums)
